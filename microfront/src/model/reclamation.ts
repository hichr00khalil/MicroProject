export interface Reclamation {
    id : number;           // L'ID de la réclamation, généralement généré par la base de données
    content : string;       // Le contenu de la réclamation (par exemple, une description détaillée)
    dateReclamation : Date;
}
